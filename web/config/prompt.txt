准则：
1. 不修改variables.scss文件
2. 修改样式时，css文件的类名使用page-title这种形式，在Jsx里面使用classes.snakeCase
3. 如果没有特意指定对应的scss文件，且该组件需要写样式时，需要单独创建componet-name.module.scss文件（component-name需要转换为小写）
4. 总体UI风格：小巧、精致（不需要特别大的边距或宽高）
5. sass由于使用了最新版本，所以引入需要@use，变量有命名空间
6. jsx里面的类处理，使用clsx库