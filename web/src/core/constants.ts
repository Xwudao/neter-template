export const KEY_TOKEN = 'token';
// clean_url_auto
export const KEY_CLEAN_URL_AUTO = 'clean_url_auto';

// roles: admin,user
// Define enum for user roles
export enum UserRole {
  ADMIN = 'admin',
  USER = 'user',
}
