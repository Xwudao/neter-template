import { clsx } from 'clsx'

/**
 * Merge class names using clsx.
 * This is a re-export for convenience, keeping a single entry point for classname utilities.
 */
export { clsx }
export default clsx
